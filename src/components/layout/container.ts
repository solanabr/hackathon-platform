/** One stage for the whole public shell — header, footer and every LP section
 * ride the same rails, gutters included, so nothing drifts a few pixels off
 * the column above it. Narrow text measures (max-w-2xl paragraphs and the
 * like) stay local to their block. */
export const PAGE_SHELL =
  "mx-auto w-full max-w-6xl px-4 sm:px-6 lg:px-8 xl:max-w-7xl xl:px-12 2xl:max-w-[88rem]";
